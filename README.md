# sql

Lancer le conteneur SQL : 

$(PWD)


docker compose up 

# Acceder au sql du conteneur sql : 

docker exec -it mysql mysql -u user -ppassword

# ROOT du conteneur SQL :

docker exec -it mysql mysql -u user -ppassword

# Acceder au sql du conteneur oracle : 

